request:
  method: POST
  path: /user/login/
  body: email=alma@alma.com&password=123
response:
  statusCode: 200

control:
  scenario:
    name: orderCreate
    requiredState:
      - not_started
      - not_authorized
    newState: authorized
